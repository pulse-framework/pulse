@tailwind base;
@tailwind components;
@css{
  body, html, #app {
    height: calc(100vh - 30px);
  }
  h1 {
    @apply text-5xl;
  }
  h2 {
    @apply text-3xl mb-4;
  }
  h3 {
    @apply text-2xl mb-4 border-b-4;
  }
  h4 {
    @apply text-xl mb-4;
  }
}
@tailwind utilities;

@import 'utilities';
@require './code'