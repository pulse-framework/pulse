const Library = require('./Pulse');

const mapData = require('./Helpers').mapData;

module.exports = {
  Library,
  mapData
};

// USE:
// import Pulse from 'pulse-framework'
// import { mapData } from 'pulse-framework'

// const Pulse = new Pulse.Library()
