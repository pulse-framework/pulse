import { Theme, ThemeKey } from './types';

export const themes: Record<ThemeKey, Theme> = {
  // dark theme
  [ThemeKey.DARK]: {
    highlight: '#efefef'
  },
  // light theme
  [ThemeKey.LIGHT]: {
    highlight: '#efefef'
  }
};
