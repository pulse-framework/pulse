{
  "private": true,
  "description": "Global state and logic framework for reactive JavaScript & TypeScript applications.",
  "author": "Jamie Pine",
  "license": "MIT",
  "syncDir": "dist",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "rimraf": "rimraf packages/**/dist",
    "build": "tsc -b packages/pulse-core",
    "build:integrations": "tsc -b packages/pulse-react",
    "test": "lerna run test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pulse-framework/pulse"
  },
  "devDependencies": {
    "@types/node": "^8.0.28",
    "lerna": "^2.9.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.9.7",
    "prettier": "2.0.5",
    "vuepress": "^1.5.4",
    "eslint-config-prettier": "^6.11.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
